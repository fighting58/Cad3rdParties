# 프로젝트 개발 및 관리 규칙 (Rules)

이 프로젝트의 일관성을 유지하고 AutoCAD 2013 환경에서의 안정성을 보장하기 위해 다음 규칙을 준수합니다.

## 1. 파일 구조 및 명령어 등록
- **상세 리습 경로**: 모든 기능별 리습(.lsp) 파일은 `lisp-src/` 폴더에 위치해야 합니다.
- **단축키 등록**: 새로운 기능을 추가하면 반드시 `Shortcuts.lsp` 파일에 다음 사항을 반영합니다.
  - 단축 명령어(Alias) 정의
  - `util:lazy-load` 함수를 적용하여 지연 로딩 처리
  - `APPHELP` 명령어의 도움말 목록 업데이트
-    * **버전 업데이트**: x.xx.xx(date)
        - 사소한 변경인 경우 버전 유지
        - 소규모 변경(lsp 추가 또는 기존 lsp 로직 변경): 마지막 자리수 증가
        - 중규모 변경(프로젝트 소규모 구조 변경): 두번째 자리수 증가
        - 대규모 변경(프로젝트 구조 변경 또는 사용자 지정): 첫번째 자리수 증가
        - version-history.md에 변경 사항을 기록한다.
     * **추가 형식**: 단축명령어 - 명령어 - 기능설명 형식으로 리스트를 작성한다.

## 2. 인터페이스 가이드라인 (프리징 방지)
- **팝업 금지**: `msgbox`, `inputbox` 등 윈도우 창을 띄우는 모든 UI 요소를 배제합니다.
- **명령행 중심**: 모든 사용자 상호작용(입력, 선택, 안내)은 AutoCAD 명령행(Prompt)을 통해서만 이루어져야 합니다.

## 3. 측량 및 기하학 표준
- **면적 부호**: 면적 계산 로직은 시계 방향(CW)을 양수(+)로 간주합니다.
- **객체 생성 규칙**: 면적 조정 등 변형이 일어나는 기능은 다른 언급이 없는 한 원본을 유지하고, **녹색(Color Index: 3)**의 `ref_object`를 새로 생성하여 결과를 표현합니다.

## 4. 코드 구현 표준
- **유틸리티 모듈화**: 공통 함수(면적 계산, 기하 연산 등)는 `util:` 접두어를 사용하여 모듈화합니다.
- **지연 로딩 (Lazy Loading)**: 메모리 효율을 위해 명령어가 실제 호출되는 시점에 파일을 로드하는 방식을 유지합니다.

## 5. 배포 및 버전 관리
- **GitHub 업로드**: 코드 업로드(Push)는 자동으로 수행하지 않으며, 사용자의 **명시적인 요청**이 있는 경우에만 수행합니다.
- **버전 기록**: 모든 변경 사항은 `RULES.md`에 정의된 버전 규칙에 따라 기록하고 관리합니다.
