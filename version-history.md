# Version History

프로젝트 주요 업데이트 및 관리 이력입니다. 모든 날짜는 한국 표준시(KST)를 기준으로 합니다.

## v1.0.12 (2026-05-18) - [Current]

### 수정 및 개선
- **AreaAdjust 고정점 모드 안정화**: `AFO2` 모드에서 P1-P2 구간을 독립적인 폴리라인으로 간주하여 오프셋(Offset) 처리하는 로직 재구축.
- **균일한 이동량 보장**: 내부 선분 교차점 연산을 통해 꺾인 구간에서도 동일한 두께의 평행이동이 적용되도록 수정.
- **P1, P2 절대 고정**: 주변 지형과의 연장 교차를 배제하고 P1, P2를 기존 좌표에 정확히 고정하여 형태 보존.
- **중간점 자동 생성**: P1, P2 사이에 정점이 없는 경우 자동으로 중간점을 생성하여 면적을 맞추도록 보완.

## v1.0.11 (2026-05-15)

### 수정 및 개선
- **REFPOLY 안정화**: `F2P/F3P` 구간 치환 로직을 정리하고, 래핑 구간(`P1>P2`) 처리 흐름을 보완.
- **방향 기준 보정**: `F3P` 실행 시 기준 폴리라인은 시계(CW) 유지, 참조 영역은 반시계(CCW) 기준이 되도록 수정.
- **VertexEdit 복구**: `VertexEdit.lsp`의 `VAD` 블록 손상/누락 상태를 복구하고 정점 추가 동작을 정상화.
- **정점 추가 규칙 개선**: `VAD`에서 클릭 좌표를 가장 가까운 선분 사이에 삽입하도록 유지하고, 기존 정점과 `0.0005m` 이내인 경우 추가하지 않도록 보완.
- **문서 업데이트**: `README.md` 기능 설명을 현재 동작(`F2P/F3P`, `VAD`) 기준으로 최신화.
- **인코딩 정비**: `README.md`, `version-history.md`를 포함한 문서/스크립트 편집 시 EUC-KR(CP949) 기준 저장 절차를 재정렬.

## v1.0.10 (2026-04-28)

### 변경 사항
- **기능 추가**: `ODLabel.lsp` 기능을 호출하는 `ODL` 단축 명령 등록.
- **도움말 업데이트**: `APPHELP` 리스트에 `ODL` (레이블 생성) 추가.

## v1.0.9 (2026-04-28)

### 변경 사항
- **기능 준수 (Compliance)**: `ArcToPolyline.lsp` 파일의 결과 개체 색상을 Green(Color Index 3)으로 수정하여 규정을 준수함.
- **표준화**: `ArcToPolyline.lsp`의 로드 메시지에서 명령어명을 제거하고 표준 형식으로 수정.
- **도움말 업데이트**: `Shortcuts.lsp`의 `APPHELP` 리스트에 `A2P`를 포함한 누락된 명령어들을 추가하고 정렬함.

## v1.0.8 (2026-04-27)

### 수정 및 개선
- **가독성 정리**: `Shortcuts.lsp`, `CoordRound.lsp`, `REFPOLY.lsp`의 섹션 구조, 들여쓰기, 주석, 블록 배치를 `AreaAdjust.lsp` 스타일에 맞춰 정리.
- **도움말 정렬 개선**: `APPHELP` 출력 항목 간격 및 정렬을 통일.
- **로드 메시지 규칙 통일**: `Shortcuts.lsp`의 파일 종료 로드 메시지를 규칙 형식으로 정리.

## v1.0.7 (2026-04-27)

### 버그 수정
- **인코딩 복구 보완**: 편집 과정에서 발생한 문자열/주석 깨짐 문제를 복구하고 주요 파일의 한글 표시를 안정화.

## v1.0.6 (2026-04-27)

### 버그 수정
- **EUC-KR(CP949) 재정렬**: `.lsp` 파일 저장 인코딩을 AutoCAD 2013 호환 형식으로 재정리.

## v1.0.5 (2026-04-27)

### 버그 수정
- **명령 호출 불일치 해결**: `Shortcuts.lsp`의 지연 로딩 호출명 불일치 수정.
- **면적 계산 일관성 개선**: `REFPOLY.lsp`의 면적 부호 계산식을 프로젝트 기준과 일치하도록 보정.

## v1.0.4 (2026-04-27)

### 수정 및 개선
- **지연 로딩 강화**: `util:lazy-load`가 경로 탐색 실패 상황에서도 보조 경로를 확인하도록 보완.
- **경로 표기 명시**: 단축 호출에서 `lisp-src/` 기준 경로를 일관되게 사용.

## v1.0.3 (2026-04-27)

### 버그 수정
- **지연 로딩 충돌 해결**: 단축명과 실제 명령명 충돌로 발생한 호출 실패 및 루프 문제 수정.

## v1.0.2 (2026-04-27)

### 버그 수정
- **MapIndex 계산 보정**: `floor` 관련 미정의 동작을 보완하여 계산 안정성 확보.

## v1.0.1 (2026-04-27)

### 수정 및 개선
- **인코딩 정책 적용**: AutoCAD 2013 호환을 위해 `.lsp` 인코딩 정책을 명확히 적용.
- **단축명 보완**: 일부 단축키 호출의 안정성 개선.

## v1.0.0 (2026-04-26)

### 신규 기능
- **VertexEdit**: 정점 추가/삭제 기능 도입 (`VAD`, `VDE`).
- **CoordRound**: 좌표 오사오입 기능 도입 (`OSA2`, `OSA3`).
- **ArcToPolyline**: Arc -> Polyline 변환 기능 도입 (`A2P`).
- **MapIndex**: 도곽 생성 기능 도입 (`WD5`, `WD10`).
- **ParcelSplit**: 지번 분해/레이어 생성 기능 도입 (`PNC`).
- **IncrementNumber**: 숫자 증분 입력 기능 도입 (`INC`).
- **OuterBoundary**: 외곽선 추출 기능 도입 (`OB`).
- **초기 통합 기능**: `REFPOLY`, `ODLabel` 연동 시작.

### 수정 및 개선
- **단축명 체계 정리**: 기본 명령과의 충돌을 피하도록 별칭 정비.
- **메시지 표준화**: 로드 완료 메시지 형식 통일.
- **지연 로딩 도입**: 초기 로드 속도와 메모리 효율 개선.
- **구조 정리**: 기능 파일을 `lisp-src/` 중심으로 구성.
