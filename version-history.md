# Version History

이 프로젝트의 주요 변경 사항 및 업데이트 이력입니다. 동일 날짜의 기록은 하나의 그룹으로 관리합니다.

## v1.0.4 (2026-04-27) - [Current]

### 수정 및 개선
- **지연 로딩 로직 강화**: `util:lazy-load` 함수가 `lisp-src/` 폴더를 자동으로 탐색하도록 개선하여 경로 설정 누락 시에도 대응 가능하도록 수정.
- **경로 명시**: `Shortcuts.lsp` 내의 모든 호출에 `lisp-src/` 경로를 명시하여 안정성 확보.

## v1.0.3 (2026-04-27)

### 버그 수정
- **지연 로딩 충돌 해결**: 단축키(WD5, WD10)와 실제 명령어 명칭이 동일하여 발생하던 무한 루프 및 로딩 실패 문제 해결. 실제 명령어를 `MAPINDEX_500`, `MAPINDEX_1000`으로 변경.

## v1.0.2 (2026-04-27)

### 버그 수정
- **MapIndex.lsp**: `floor` 함수 미정의 오류 수정 (`util:floor` 구현 및 적용).

## v1.0.1 (2026-04-27)

### 수정 및 개선
- **인코딩 적용**: 모든 리습(.lsp) 및 관련 파일의 인코딩을 EUC-KR(CP949)로 변환 (Rule 30 준수).
- **단축키 교정**: WD5, WD10 단축키가 실제 작동하지 않던 문제 수정 (`Shortcuts.lsp` 및 `MapIndex.lsp` 업데이트).

## v1.0.0 (2026-04-26)

### 신규 기능
- **정점 편집 도구 (VertexEdit.lsp)**: 정점 추가(VAD) 및 삭제(VDE) 기능. (원본 직접 수정 방식)
  - `VAD`: 정점 추가. LINE 선택 시 LWPolyline으로 자동 변환.
  - `VDE`: 정점 삭제. Endpoint OSNAP 자동 활성화 및 0.005 오차 범위 적용.
- **좌표 오사오입 도구 (CoordRound.lsp)**: 소수점 2자리(OSA2) 및 3자리(OSA3) 오사오입 정결합 기능.
- **호 변환 도구 (ArcToPolyline.lsp)**: 호(Arc)를 현분 폴리라인으로 변환(A2P). (N, M, D 옵션 지정)
- **도곽 생성 도구 (MapIndex.lsp)**: 원점 기준 1:500(WD5), 1:1000(WD10) 도곽 자동 생성.
- **지번 데이터 분해 도구 (ParcelSplit.lsp)**: 지번 문자에서 본번/부번 지번 및 도표구분을 추출하여 레이어별 생성(PNC).
- **자동 숫자 증가 도구 (IncrementNumber.lsp)**: 클릭 지점에 숫자를 1씩 증가시키며 텍스트 입력(INC).
- **외곽선 추출 도구 (OuterBoundary.lsp)**: 선택한 객체들을 포함하는 최외곽 경계선 추출(OB).
- **초기 핵심 기능**: 
  - **REFPOLY**: 폴리라인 방향 정렬 및 정규화 (F2P, F3P).
  - **ODLabel**: 객체 데이터(OD) 추출 및 자동 회전 레이블 생성 (ODL).

### 수정 및 개선
- **단축키 최적화**: 
  - `PS`를 `PNC`로 변경 (AutoCAD PSPACE 명령어 충돌 방지).
  - `WD500/WD1000`을 `WD5/WD10`으로 단축키 간소화.
  - `VA/VD`를 `VAD/VDE`로 변경 (내장 명령어 충돌 방지).
  - `A2P` 등 모든 리습의 메인 함수명을 고유 네임스페이스로 변경 및 단축키 지연 로딩 연결.
- **메시지 표준화**: 
  - 모든 리습 파일의 로딩 메시지를 `{기능 요약} 로드 완료.` 형식으로 통일.
- **시스템 최적화**: 
  - **지연 로딩 (Lazy Loading)** 도입으로 초기 구동 속도 및 메모리 사용량 최적화.
  - 리습 파일들을 `lisp-src/` 폴더로 구조화하여 관리 효율성 증대.
- **규칙 및 도움말**: 
  - `RULES.md` 정립 (프리징 방지 가이드, 버전 관리 명명 규칙 등).
  - `APPHELP` 명령어 고도화 (정형화된 기능 리스트 출력).
